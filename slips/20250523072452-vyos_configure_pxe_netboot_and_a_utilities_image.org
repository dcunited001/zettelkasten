:PROPERTIES:
:ID:       8211ee55-ff54-4e7e-8907-d273f6baafc5
:END:
#+TITLE: VyOS: Configure PXE, Netboot and a Utilities Image
#+CATEGORY: slips
#+TAGS:

* Roam
+ [[id:5aa36ac8-32b3-421f-afb1-5b6292b06915][VyOS]]
+ [[id:b82627bf-a0de-45c5-8ff4-229936549942][Guix]]
+ [[id:ea11e6b1-6fb8-40e7-a40c-89e42697c9c4][Networking]]
+ [[id:e967c669-79e5-4a1a-828e-3b1dfbec1d19][Route Switch]]
+ [[id:bdae77b1-d9f0-4d3a-a2fb-2ecdab5fd531][Linux]]

* Docs

* Resources
** Vyos
+ [[https://docs.vyos.io/en/rolling/installation/install.html#pxe-boot][Install: PXE Boot]] includes TFTP -> HTTP instructions

** Pixiecore
+ [[https://github.com/danderson/netboot/tree/main/pixiecore][danderson/netboot]]

** Netboot

+ [[https://netboot.xyz/docs/booting/tftp/][(iPXE) Boot from TFTP]]

*** Images
kpxe (x86_64 only)

+ https://boot.netboot.xyz/ipxe/netboot.xyz.kpxe

x86_64

+ [[https://boot.netboot.xyz/ipxe/netboot.xyz.efi]]
+ [[https://boot.netboot.xyz/ipxe/netboot.xyz-snp.efi]]
+ [[https://boot.netboot.xyz/ipxe/netboot.xyz-snponly.efi]]

aarch64

+ [[https://boot.netboot.xyz/ipxe/netboot.xyz.efi]]
+ [[https://boot.netboot.xyz/ipxe/netboot.xyz-arm64.efi]]
+ [[https://boot.netboot.xyz/ipxe/netboot.xyz-arm64-snp.efi]]
+ [[https://boot.netboot.xyz/ipxe/netboot.xyz-arm64-snponly.efi]]
** Guix

Guix makes iPXE =.efi= files available in the =ipxe= and =ipxe-guix= packages.

+ These build from source [[https://codeberg.org/guix/guix/src/commit/2acb5f9da33586b20cb49aeb60d5adcd7468e1fc/gnu/packages/bootloaders.scm#L1887][./gnu/packages/bootloaders.scm]].
+ =x64_64= and =i386= are supported

#+begin_verse
├── etc/profile
├── lib -> /gnu/store/fmddnh81wfi468b32amnxl8wqdc8l7fq-ipxe-1.21.1-4.969ce2c/lib
│   └── ipxe
│       ├── 10222000.rom
│       ├── 10500940.rom
│       ├── 10ec8139.rom
│       ├── 15ad07b0.rom
│       ├── 1af41000.rom
│       ├── 3c509.rom
│       ├── 8086100e.mrom
│       ├── 8086100f.mrom
│       ├── 808610d3.mrom
│       ├── 80861209.rom
│       ├── i386-efi
│       │   ├── ipxe.efi
│       │   └── ipxe.efirom
│       ├── intel.mrom
│       ├── intel.rom
│       ├── ipxe.iso
│       ├── ipxe.lkrn
│       ├── ipxe.pxe
│       ├── ipxe.usb
│       ├── rtl8139.rom
│       ├── undionly.kpxe
│       ├── x86_64-efi
│       │   ├── ipxe.efi
│       │   └── ipxe.efirom
│       └── x86_64-pcbios
│           ├── 8086100e.mrom
│           ├── intel.rom
│           ├── ipxe.pxe
│           ├── ipxe.usb
│           └── undionly.kpxe
├── manifest
└── share...
#+end_verse

* Networking

The Netboot images either need to be available on the system's disk -- but the
whole point is to not need a disk.

* Pixiecore

* Vyos

** Layer 2

** Layer 3

** Layer 4

* Guix

#+begin_src scheme :tangle ~/.config/guix/systems/pxe-bootstrap.scm
(use-modules (ellipsis system usb-gpg-tools)
             (srfi srfi-1)
             (gnu)
             (gnu system)
             ;; (gnu system nss)
             ;; (gnu system pam)

(nongnu packages linux)
(nongnu system linux-initrd)

  #:export (usb-gpg-tools
            usb-gpg-tools-amd
            pxe-tools))

(service static-networking-service-type
         (list (static-networking
                (addresses
                 (list (network-address
                        (device "eno1")
                        (value "10.0.2.15/24"))))
                (routes
                 (list (network-route
                        (destination "default")
                        (gateway "10.0.2.2"))))
                (name-servers '("10.0.2.3")))))

(define pxe-boot-tools
  (operating-system
    (inherit pxe-boot-tools-amd)
    (host-name "pxeboottools-amd")
    (timezone "America/New_York")
    (locale "en_US.UTF-8")

    ;; NONFREE
    (kernel linux)
    (firmware (cons* ;; linux-firmware
               amd-microcode
               amdgpu-firmware
               realtek-firmware
               %base-firmware))

    (kernel-arguments '("modprobe.blacklist=radeon"
                        ;; "quiet" ;; .....
                        ;; "net.iframes=0"
                        ))))
#+end_src
** Evaluate

+ OpenVSwitch supports: NetFlow, sFlow, IPFIX, RSPAN, CLI, LACP, 802.1ag
