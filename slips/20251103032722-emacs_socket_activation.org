:PROPERTIES:
:ID:       04c73b2f-0965-4dc9-adc7-d6cddd8f4e75
:END:
#+TITLE: Emacs: Socket Activation
#+CATEGORY: slips
#+TAGS:  

* Roam
+ [[id:6f769bd4-6f54-4da7-a329-8cf5226128c9][Emacs]]
+ [[id:df7f060a-d663-4eaa-844e-f8baec7c94a2][SystemD]]
+ [[id:dae42c2e-aba7-4c9c-9878-b9665befe205][IPC And Sockets]]
  
* Resources
+ Debian codesearch for [[https://codesearch.debian.net/search?q=pkg%3Aemacs+sd_listen_fds][emacs package]]. I didn't realize Debian had this.

** Emacs

*** Slices

[[https://leon.schuermann.io/blog/2023-11-12_emacs-service-systemd-slices.html][Running Emacs (with EXWM) in systemd's session.slice]] slicing is more important
for EXWM.

*** Socket activation handling

+ [[https://git.savannah.gnu.org/cgit/emacs.git/tree/lisp/server.el?h=master#n255][server-name]] according to the docs, this can be either the name of a socket
  under =XDG_RUNTIME_DIR= or the path of a socket file.
  - server-name gets set to [[https://git.savannah.gnu.org/cgit/emacs.git/tree/lisp/server.el?h=master#n256][internal--server-name]] when an external =sockfd= is
    passed into the intializing =emacs= process (via =process.c=, via =emacs.c=)
  - sockname is validated [[https://git.savannah.gnu.org/cgit/emacs.git/tree/src/process.c?h=master#n8671][init_process_emacs(int sockfd)]] from =sockfd=
  - =init_process_emacs(sockfd)= is called in =main(int argc, char **argv)=
+ some of the line numbers above were out of date when i generated the links.

In other words, if =emacs= detects that systemd initiated it, it probably won't
care about the arguments to =--fg-daemon=.
** SystemD

+ [[https://www.freedesktop.org/software/systemd/man/latest/systemd.socket.html][systemd.socket]]
+ [[https://man7.org/linux/man-pages/man1/systemd-socket-activate.1.html][systemd-socket-activate]]: Test socket activation
+ [[https://www.freedesktop.org/software/systemd/man/latest/sd_listen_fds.html#][sd_listen_fds]] emacs calls this to check with systemd for sockets passed as
  file descriptors.

* Notes

Check =system-configuration-features= to ensure your emacs build includes
=LIBSYSTEMD=. [[https://sources.debian.org/src/emacs/1:30.1+1-9/src/emacs.c?hl=1796#L1794-L1807][This section of emacs.c]] is otherwise removed.
*** TODO finish emacs socket activation

** Socket Activation

SystemD creates a process that owns the socket specified by =someservice.socket=
When a service that requires or relies on this socket becomes activated, systemd


** Core Pinning


*** TODO complete socket activation, then add slice/scope configuration to pin

+ [ ] emacs$svr.scope :: =CPUAffinity=7,8=
  - see =man systemd.exec= and =man systemd.scope=
+ [ ] chromium.scope :: =CPUAffinity=17-32=
  - doing this is probably unstable
  - really =qubes= is doing it right...

To really benefit from this:

+ You can't have other processes evicting L2 cache.
+ You also need control over the processes it launches (effectively as
  complicated as "Running Emacs with EXWM in systemd's session slice", or with
  UWSM)
  - You don't _always_ want the processes to launch on the same core though.
  - For Emacs, you want the core pretty much reserved for it's own processes.
    IDK whether pinning =emacsclient= results in benefits though

There's another problem (a bit naive)

+ RAM i/o changes when processes pinned to CPUs
+ especially with =CPUAffinity=numa= and =Numa
