:PROPERTIES:
:ID:       f6350f65-05c7-4a87-bfd1-0a7962e30190
:END:
#+TITLE: Linux: Repology
#+DESCRIPTION: 
#+TAGS:
* Roam

* Docs
** API
Repology API also offers [[https://dumps.repology.org/README.txt][daily =postgresql= dumps]]. nice.

+ [[https://repology.org/api/v1][api/v1]] docs


* Resources

* Topics
** API

** DB Dumps
:PROPERTIES:
:header-args+: :dir /data/repo/repology
:END:

Takes a while

#+begin_src shell 
d=$(date +%F)
name=$d.sql
curl -s https://dumps.repology.org/repology-database-dump-latest.sql.zst > db/$name.zst
#+end_src

The database is downloaded over a rather slow connection, but it's all the data
in once place..... (one hour layterr)... okay it's =321M= now and still going.

The whole thing is 2GB, but 15GB uncompressed..

#+begin_src shell 
d=$(date +%F)
name=$d.sql
cat db/$name.zst | zstd -d > db/$name
#+end_src

#+RESULTS:

* Issues
