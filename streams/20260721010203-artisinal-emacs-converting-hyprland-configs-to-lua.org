:PROPERTIES:
:ID:       77ef19af-b6d5-4b87-b79c-ee779565f0df
:END:
#+title: Artisinal Emacs: 

* Roam

+ [[id:c2afa949-0d1c-4703-b69c-02ffa854d4f4][GnuPG]]
+ 

* Stream

** Youtube

Japanese Interface Elements

+ チャンネル登録者 :: only allow subscribers to chat
+ ライブ配信を開始 :: begin live-stream
  - 準備ができたようです。配信を開始するにはこちらをクリックします。
  - ライブ配信を終了 :: end live-stream

** Chapters

1:30 org-capture: set up a "tangent stack" outline
6:15 (idea) org-crypt: store links out to LLM generated docs
7:15 setting boundaries for LLM-generated content in a PIM
8:30 hyprland: potential issues running a nested wayland client
12:30 org-crypt: for LLM links
13:15 (demo) lispy.el: shuffle around code in emacs gpg functions
15:15 GPG: interview questions ideas: contexts, identities, recipients and rekeying
16:00 GPG: practical issues managing subkeys to your GPG identity
17:20 GPG: overview of rekeying encrypted material without decrypting to disk
19:10 Scripting: managing subtrees of selected filepaths without mkdir
24:40 org-crypt: return to usage for LLM links
25:00 (mistake) restricting an alphabet to hexidecimal probably weakens crypto
25:15 GPG: sha256sum salt to extend entropy and AES blocks
26:15 (forgot) to demo `C-u M-| gpg --list-packets` to mention message key derivation
26:45 org-crypt: finding all usages in roam (to rekey later with org-element)
27:40 org-roam: searching for jq CLI usage
28:30 jq: query to recovere the "shape" of object properties in JSON subtrees
30:15 PIM: curating notes for memory in a personal information management system
33:45 pandoc: juggle markdown in org-mode (see agzam/prisma.el for a better way)
35:45 (idea) hyprland: use a separate modifier key for a nested hyprland instance
37:00 xkb: overview of a config for my $10 keyboard to get a free modifier key!
38:00 (demo) jq: quick conversion of evdev.xml to json to yaml with xq and jq
38:45 xkb: how evdev.xml corrsponds to xkb's evdev
40:10 xkb: setting up MOD3 and ALTGR on a $10 keyboard with xkb
41:50 hyprland: quick overview of my hyprland workspaces layout
47:00 org-agenda: trying to find a lost org-log configuration
48:50 hyprland: overview of migration process from hyprlang to lua
51.05 hyprland: retaining descriptions for hyprland bindings (so i can search with walker)

*** Remaining

52:00 why use both omarchy and nixos?
53:15 using watch and watchexec generally
54:45 (demo) using watch with ss to see all :443 TCP connections
56:15 using watch and watchexec with hyprland --validate-config


mention using uvx to run Prateek-squadron/hyprconf2lua

#+begin_example 
jq -r '.events | map([.tStartMs/1000.00, ((.segs // empty) | map(.utf8) | join(""))] | @tsv) | join("\n")'
#+end_example

#+begin_src jq :stdin timedtext :cmd-line -r
.events
  | map([.tStartMs/1000.00,
         ((.segs // empty) | map(.utf8) | join(""))
        ] | @tsv)
  | join("\n")
# todo: replace literal \n in text ... grep -F '\\?n' no worky
#+end_src

